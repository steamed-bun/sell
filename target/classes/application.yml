spring:
  datasource:
      driver-class-name: com.mysql.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/imooc?useSSL=false&characterEncoding=utf-8
      username: root
      password: root8888
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5Dialect
logging:
  pattern:
    console: "%d - %msg%n"
  path: var/log/tomcat/
server:
  context-path: /sell
